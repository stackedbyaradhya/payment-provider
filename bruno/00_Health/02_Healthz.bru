meta {
  name: Liveness Probe
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/healthz
  body: none
  auth: none
}

tests {
  test("200 ok", function () {
    expect(res.getStatus()).to.equal(200);
    expect(res.getBody().status).to.equal("ok");
  });
}
